# Postgres password (generate with: openssl rand -hex 24)
POSTGRES_PASSWORD=

# Cloudflare Tunnel connector token for the "flogg" tunnel (secret)
TUNNEL_TOKEN=

# Set to false to block new registrations (the first account is always allowed and becomes admin)
ALLOW_SIGNUPS=true
